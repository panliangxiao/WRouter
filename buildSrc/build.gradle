apply plugin: 'groovy'

apply from: 'maven_build.gradle'

dependencies {
    compile gradleApi()
    compile localGroovy()
    compile 'com.android.tools.build:gradle:3.0.0'
}

repositories {
    google()
    jcenter()
}